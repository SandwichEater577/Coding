# Custom interactive shell — pipes, redirects, builtins, history
[package]
name = "jewos-shell"
version.workspace = true
edition.workspace = true

[[bin]]
name = "shell"
path = "src/main.rs"

[dependencies]
nix = { version = "0.29", features = ["process", "signal", "fs"] }
rustyline = "15"
