# JewOS — Custom Linux distro with a pure-Rust userspace
[workspace]
members = ["init", "shell", "utils", "tui"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"

[profile.release]
opt-level = "s"
strip = true
lto = true
