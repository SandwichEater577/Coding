# fibonacci.nest — recursive fibonacci sequence

fn fib(n) {
    if n <= 1 { return n; }
    return fib(n - 1) + fib(n - 2);
}

println("=== Fibonacci Sequence ===");
for i in 0..20 {
    println("fib(" + str(i) + ") = " + str(fib(i)));
}
