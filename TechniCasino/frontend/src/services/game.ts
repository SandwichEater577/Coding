// ── services/game.ts — Game API service ──
// getGames() → list available games
// getGame(id) → single game details
// createSession(gameId) → start a game session
// getHistory() → user's game history
