// ── services/websocket.ts — WebSocket client ──
// connect(path) → establish WS connection
// send(event, data) → send message to server
// on(event, callback) → listen for server messages
// disconnect() → close connection
// Auto-reconnect logic.
