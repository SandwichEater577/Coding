// ── utils/validators.ts — Input validation helpers ──
// isValidEmail(email)
// isValidPassword(password) → min 8 chars, uppercase, number
// isValidUsername(username)
// isValidAmount(amount) → positive number within limits
