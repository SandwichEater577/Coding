// ── ws/session.rs — WebSocket session handler ──
// Manages an individual player's WS connection.
// Receives game actions (bet, hit, stand, fold, etc.).
// Sends game state updates back to the client.
