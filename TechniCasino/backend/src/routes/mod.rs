// ── routes/mod.rs — Route aggregator ──
// Combines all route modules into the main Router.

pub mod auth;
pub mod game;
pub mod wallet;
pub mod user;

// TODO: pub fn router(state: AppState) -> Router { ... }
